#!/bin/bash

# Simple bash script to build the GNOME Shell extension
#echo "Zipping the extension..."
echo "ZIP Build not supported"
#zip -r easy_docker_containers@red.software.systems.zip . -x *.git* -x *.idea* -x *.history* -x *.*~ -x build.sh -x *.vscode/*
#zip -r easy_docker_containers@red.software.systems.zip . -x *.git* -x *.idea* -x *.history* -x *.*~ -x build.sh -x *.vscode/*

#echo "Building is done."
